#!/usr/bin/env bash

## Description: Unavailable — use tools/quickstart.sh from a GEO Starter checkout.
## Usage: geo-install
## Example: "ddev geo-install"
## ExecRaw: false

set -euo pipefail

# This command deliberately does not attempt the obsolete installer. Its first
# end-to-end run found incompatible Composer, Drush, recipe-path, database, and
# docroot assumptions. Continuing after a warning made the failure destructive
# and ambiguous. Keep this stub until a separately validated redesign replaces
# it.
echo "error: ddev geo-install is unavailable; it is not an install path." >&2
echo "Run tools/quickstart.sh <directory> [tag] from a GEO Starter checkout." >&2
exit 64
