services:
  geo_starter_mcp.content_model:
    class: Drupal\geo_starter_mcp\ContentModel
    arguments:
      - '@entity_field.manager'
      - '@entity_type.bundle.info'
  geo_starter_mcp.draft_writer:
    class: Drupal\geo_starter_mcp\DraftWriter
    arguments:
      - '@entity_type.manager'
      - '@geo_starter_mcp.content_model'
